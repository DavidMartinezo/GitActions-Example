name: Example

on:
 workflow_dispatch: 
 pull_request: 
  branches:
     - main
  # push:
  #   branches: 
  #     - main
  # schedule:
  #   - cron: "*/5 * * * *" # Runs every 5 minutes  
#there are 3 kinds of triggers in Github Actions, Webhooks, schedule and manual.
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Displays a message
        run: echo "This changes are affecting the main branch"

      - name: Another task
        run: echo "Another Task"
        
      - name: Checkout
        uses: actions/checkout@v6.0.3

      - name: List the repo files
        run: | 
              echo "The repo ${{github.repository}} contains the following Files: "
              tree

        
        
